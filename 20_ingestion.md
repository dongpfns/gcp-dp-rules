# 20. データ取込方式（ELT/ETL・外部連携・更新方式）

## 1. 基本方針：ELTを原則とする

**変換はBigQuery内で行う（ELT）。取込処理は「運ぶ」だけに徹する。**

| | ELT（原則） | ETL（例外） |
| :--- | :--- | :--- |
| 変換の場所 | BigQuery（SQL） | 取込処理内（Python） |
| 再実行 | BRONZEから何度でも再現できる | ソースへ再アクセスが必要 |
| ロジックの可視性 | Dataform/dbtのリネージに載る | コードを読まないと分からない |

**ETL（取込前変換）を許容する3ケースのみ**

1. **個人情報のマスキング／ハッシュ化** — 生値をBigQueryに置いてはいけない要件がある場合。
2. **フォーマット変換** — BigQueryが直接読めない形式（XML、Excel、固定長、独自バイナリ、暗号化ファイル）を JSONL / CSV / Parquet に変換する。**構造の変換のみで、値の加工はしない**。
3. **列の間引き** — ソースが極端に横長（数千列）で、明らかに不要な列を落とさないとコストが見合わない場合。落とした列はADRに記録する。

上記以外の変換（型変換・名寄せ・結合・集計・コード変換）を取込処理に書くことは**禁止**する。

## 2. 外部連携方式

### 2.1 連携元ごとの標準方式

| 連携元 | 標準方式 | 補足 |
| :--- | :--- | :--- |
| **Salesforce** | ① BigQuery DTS（Salesforceコネクタ）<br>② SaaS ETL（trocco / Fivetran）<br>③ Cloud Functions + Bulk API 2.0 | ①②が使えるなら自前実装しない。③は対象オブジェクトが少ない／DTS非対応項目がある場合のみ |
| **Box** | Cloud Functions + Box API（JWT / CCG認証）→ GCS raw → BQロード | Boxのフォルダを直接BigQueryから読む手段はない。必ずGCSを経由する |
| **外部API（REST）** | Cloud Functions (Python) → GCS raw（JSONL）→ BQロード | ページング・レート制限・リトライは取込処理の責務。9分/60分を超えるなら Cloud Run Jobs |
| **CSV / 固定長ファイル** | 授受先（SFTP/共有ストレージ/メール添付）→ GCS raw → BQロード | 文字コード・改行コード・区切り文字を必ず仕様として明文化する |
| **オンプレDB** | Datastream（対応DB）または Cloud Run Jobs + JDBC → GCS | Datastreamが使えるならCDCログベースが最も安全 |
| **GCS上に既にあるファイル** | Eventarc（オブジェクト作成）→ Workflows → BQロード | ポーリングではなくイベント駆動 |

#### 2.1.1 コネクタの取込特性を先に確認する

**どのツールを使うかを決めた時点で、選べる更新方式（3節）はほぼ決まる。**
データ量から方式を決める前に、そのツールが何を提供しているかを必ず確認し、設計書に書く。
ここを飛ばすと「取得は毎回全件なのに、マージだけ差分ウィンドウで絞る」といった
**取得範囲とマージ範囲がずれた設計**（`90` 章 落とし穴）を作り込む。

| 確認項目 | なぜ必要か |
| :--- | :--- |
| 提供される取込方式（全件 / 差分 / CDC） | 3.2の選定フローの入力になる |
| 差分の指定方法（基準列 / 前回実行時点 / 期間指定） | 「N日遡る」を指定できないツールは多い。取得範囲とマージ範囲を揃えられるかがここで決まる |
| 削除の同期可否 | 論理削除フラグの設計（`21` 章4節）が変わる |
| 提供状態（GA / プレビュー）と、後から変更できない設定 | プレビュー機能の本番採用可否と、初期設定をやり直すコスト |

**調査済みのコネクタ**（新たに調べたらこの表に追記する）

| コネクタ | 取込方式 | 差分の指定方法 | 削除同期 | 状態・制約 |
| :--- | :--- | :--- | :--- | :--- |
| BigQuery DTS / Salesforce | 完全 または 増分 | 増分はウォーターマーク列（TIMESTAMP型・単調増加。`SystemModstamp` 推奨）による「前回転送以降」。**日数ウィンドウの指定は不可** | 増分は**不可**。完全なら BRONZE が毎回ソースの完全な状態になるため `21` 章4.2で検知できる | 増分は**プレビュー**。増分の初回実行後はアセット・書き込みモード・ウォーターマーク列・主キーを変更できない（変更には転送構成の作り直しが必要） |

### 2.2 共通の実装規約（取込処理）

1. **必ずGCSに原本を落としてからBigQueryにロードする**。APIレスポンスを直接BigQueryへ `insert` しない（再現性が失われ、失敗時に途中まで入る）。
   * 例外: DTS / Datastream などGoogleマネージドの転送。
2. **冪等性**: 同じ `run_id` で再実行しても結果が同じになること。GCSは `{run_id}` でパスを分け、BigQueryは `WRITE_TRUNCATE`（パーティション指定）または `MERGE` で書く。
3. **リトライ**: HTTP 429 / 5xx は指数バックオフ（初回1秒、最大5回、ジッタ付き）で再試行する。4xx（429以外）は即座に失敗させる。
4. **タイムアウト**: 外部API呼び出しには必ずタイムアウトを設定する（接続10秒 / 読み取り60秒を既定）。
5. **認証情報は Secret Manager から取得**する。環境変数への直書き・コードへの埋め込みは禁止（`30` 章）。
6. **取得件数0件は「正常」ではなく「要判定」**。0件が想定される連携かどうかを設定で持ち、想定外の0件はエラーにする。
7. **構造化ログ**（JSON）で `run_id` / `source_system` / `object` / `record_count` / `duration_ms` を出力する（`60` 章）。
8. スキーマはスキーマJSONファイルとしてGit管理し、ロード時に明示指定する。

### 2.3 取込処理のインターフェース（標準）

取込用Cloud Functions / Cloud Run Jobs は、以下のパラメータを受け取る形に統一する。

| パラメータ | 必須 | 意味 |
| :--- | :--- | :--- |
| `run_id` | ✓ | パイプライン実行ID（`ETL_BATCH_ID` と同値） |
| `object` | ✓ | 取得対象オブジェクト名 |
| `mode` |  | `full` / `incremental`（既定は設定ファイルの値） |
| `start_date` / `end_date` |  | 差分・再取込の対象期間（YYYY-MM-DD、JST基準） |

戻り値（JSON）: `{"status": "success", "record_count": 1234, "gcs_uri": "gs://...", "run_id": "..."}`

## 3. 更新方式（全件 / 差分）

### 3.1 パターン一覧

| # | パターン | BRONZEへの書き方 | SILVERへの反映 | 物理削除の検知 | 向くケース |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **P1** | **全件洗替**（FULL_SNAPSHOT） | `WRITE_TRUNCATE` | `MERGE`（全件） | **可** | 数十万件以下のマスタ。最も安全 |
| **P2** | **範囲洗替**（ROLLING_WINDOW_SNAPSHOT） | 対象パーティションのみ `WRITE_TRUNCATE` | 範囲限定 `MERGE` | **範囲内のみ可** | 直近N日/Nか月が毎回全量再送されるトランザクション |
| **P3** | **差分追記**（INCREMENTAL_APPEND） | `WRITE_APPEND` | `INSERT`（重複排除付き） | 不可 | イベントログ、追記のみで更新も削除もないデータ |
| **P4** | **差分マージ**（INCREMENTAL_MERGE） | `WRITE_APPEND` | `MERGE`（キー一致で更新） | 不可 | 更新はあるが削除がない／削除フラグをソースが持つマスタ |
| **P5** | **CDCログ**（CDC_LOG） | `WRITE_APPEND`（変更ログ） | ログの `op` 種別に従い `MERGE` | **可**（削除イベントで検知） | Datastream等でDBの変更ログを受け取れる場合 |

### 3.2 選定フロー

**フローに入る前に、2.1.1 で取込ツールの取込特性を確認すること。**
ツール側の制約が方式を規定するため、データ量から入ってはいけない。

```text
【0】取込ツールが差分取得を提供しているか？（2.1.1）
  ├─ No ／ プレビュー段階 ／ 取得範囲を制御できない ──► P1 全件洗替
  │        （規模がP1に収まらないなら、ツールの選定からやり直す）
  └─ Yes
      │
      ソースが変更ログ（INSERT/UPDATE/DELETE）を出せる？
        ├─ Yes ─────────────────────────────► P5 CDCログ
        └─ No
            毎回全件を送れる規模（目安：1000万行未満／1GB未満）？
              ├─ Yes ───────────────────────► P1 全件洗替
              └─ No
                  直近N期間は「確定値・訂正値を含めて毎回全量」再送される？
                    ├─ Yes ─────────────────► P2 範囲洗替
                    └─ No
                        レコードの更新が発生する？
                          ├─ Yes ───────────► P4 差分マージ（削除は検知できない）
                          └─ No ────────────► P3 差分追記
```

* 【0】でP1に落ちた場合、**差分ウィンドウ（遡り日数）という変数を設計に持ち込まない**。
  取得が全件なのにマージだけ絞る構成は、コストが下がらないうえに
  取得範囲とマージ範囲のずれ（`90` 章）そのものになる。

### 3.3 各パターンの必須事項

**P1 全件洗替**
* BRONZEはパーティションを持たず毎回洗替でよい。取得日を `_bronze_ingested_at` で追跡する。
* SILVERは `MERGE` の `WHEN NOT MATCHED BY SOURCE THEN UPDATE SET is_deleted = TRUE` で削除検知する。
* **★必須★ MERGEの前に、BRONZE件数の「下限」と「前回比の急減」を必ず検査し、
  外れたら後続を止める。**
  P1は「最も安全」だが、それは**取得が成功している限り**の話である。
  取得が失敗してBRONZEが空・欠損になった状態でMERGEを流すと、
  `WHEN NOT MATCHED BY SOURCE` によって**SILVERの全行が一括で論理削除される**。
  P2の範囲ずれ事故より復旧が重い。

  | # | 検査 | 既定の閾値 |
  | :--- | :--- | ---: |
  | ① | BRONZE件数 ≧ 下限件数（0件を「正常」にしない） | 1件 |
  | ② | BRONZE件数 ≧ SILVERの有効件数（`is_deleted = FALSE`）× 比率 | 0.5 |

  * ②は初回実行（SILVERが0件）では発動しない。
  * **閾値を下げて押し通す運用を禁止する。** 件数が減った理由を業務側に確認してから、
    一時的に閾値を変更し、完了後に必ず戻す。手順を各パイプラインのREADME／設計書に書く。

**P2 範囲洗替（最も事故が多い）**
* **範囲条件を「3か所すべて」に同一の式で書く**（`21` 章 4節）。
  1. ソース側サブクエリの `WHERE`
  2. `MERGE ... ON` 句
  3. `WHEN NOT MATCHED BY SOURCE AND ...` 句
* **ウィンドウはソース側の処理単位の境界に丸める**。月単位で抽出しているのに日単位ウィンドウ（`CURRENT_DATE() - 120 DAY`）で切ると、境界月の月初数日だけが範囲外に落ち、重複INSERTされる。月単位なら `DATE_TRUNC(..., MONTH)`。
* **取得範囲とマージ範囲を必ず同じ期間に揃える**。片方だけ広げても意味がない。
* **ウィンドウ幅は「ソースが遡って訂正しうる最大期間」より長くする**。
* ウィンドウ外に更新が届いたことを検知するアサーション／テストを常設する。

**P3 差分追記**
* 重複が必ず起きる前提で設計する。SILVERで `QUALIFY ROW_NUMBER() OVER (PARTITION BY key ORDER BY event_at_utc DESC) = 1` により最新行を採る。
* BRONZEは追記のみのため、再実行しても同じ行が増えないよう `run_id` を列に持ち、SILVER側で除外できるようにする。

**P4 差分マージ**
* 差分の基準はソース側の更新日時列。**基準列にインデックス/絞り込みが効くこと**をソース側と合意する。
* **削除は検知できない**。ソース側が削除フラグ（論理削除）を持つ場合はそれを `is_deleted` に写す。持たない場合は、月次などで P1 の全件洗替を併用して整合を取る（「定期フル同期」の運用を必ず設計に入れる）。

**P5 CDCログ**
* 同一キーに対する複数変更が1バッチに含まれるため、`MERGE` 前に**キーごとの最終イベントだけを採る**。
* 削除イベント（`op = 'DELETE'`）は `is_deleted = TRUE` にする。物理削除はしない。

## 4. 再実行・バックフィル

1. **再実行は必ず「期間指定」で行えるようにする**。`start_date` / `end_date` を受け取らないパイプラインを作らない。
2. バックフィルは**パーティション単位**で実行する。単一ジョブで4,000パーティションを超える変更はできない（`11` 章 3.1）。
3. バックフィル時の一時テーブルは `stg_` を付け、完了後に必ず削除する。
4. **バックフィル手順を各パイプラインのREADMEに書く**。「誰が・どのコマンドで・どの範囲を・どれくらいの時間で」再実行できるかが書かれていないパイプラインはレビューを通さない。

## 5. データ品質チェック（取込直後）

BRONZEロード直後に、最低限以下を機械的に検証し、失敗したら後続を止める。

| チェック | 内容 |
| :--- | :--- |
| 件数 | 取得件数が0でないこと（0件許容の設定がある場合を除く） |
| 件数の急変 | 前回実行比で ±50% 以上変動していないこと（閾値はテーブルごとに設定） |
| 必須列 | 主キー相当列にNULLがないこと |
| 主キー重複 | P1/P2 の場合、キーの重複がないこと |
| 鮮度 | ソース側の最大更新日時が想定範囲内であること |

* **P1 全件洗替の場合、「件数」と「件数の急変」は任意ではなく必須**とする（3.3）。
  このチェックが取得失敗とSILVERの一括論理削除の間に立つ唯一の安全装置になる。

詳細な実装は `60` 章（データ品質）および `21` 章（assertions / tests）を参照。

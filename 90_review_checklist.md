# 90. レビューチェックリスト・落とし穴集

## 1. 事故りやすい落とし穴 TOP12

レビュー時に**必ず確認する**項目。過去に実際に事故になった、または事故になりうるもの。

| # | 落とし穴 | 何が起きるか | 参照 |
| :--- | :--- | :--- | :--- |
| 1 | **範囲限定MERGEで、範囲条件を `ON` 句だけに書く** | 範囲外の古い行が「ソースに存在しない」と判定され、**過去データが一括で論理削除**される | `21` 4.3 |
| 2 | **範囲条件をソース側の `WHERE` に書き忘れる** | ソース側だけ範囲外の行を含み、`NOT MATCHED` → **INSERT** で重複行になる | `21` 4.3 |
| 3 | **全件洗替（P1）で、件数ガードなしに `MERGE` を流す** | 取得が失敗してBRONZEが空・欠損の状態でMERGEが走ると、`NOT MATCHED BY SOURCE` により**SILVERの全行が一括で論理削除**される。「P1は最も安全」は取得が成功している限りの話 | `20` 3.3 |
| 4 | **範囲ウィンドウをソースの処理単位と違う粒度で切る** | 月抽出なのに日ウィンドウ → 境界月の月初数日だけが範囲外に落ち重複。境界が毎日動くため**再現性の低い障害**になる | `21` 4.3 |
| 5 | **10年以上保持するテーブルを日単位パーティションにする** | 約11年で「単一ジョブ4,000パーティション上限」に当たり、**フルリフレッシュが実行不能**になる。粒度は後から変えられない | `11` 3.1 |
| 6 | **BigQueryを呼ぶステップで `jobComplete` を検査しない** | `jobs.query` はタイムアウト時に**エラーではなく `jobComplete: false` を返す**。ワークフローは成功扱いで進み、**「成功したのにデータが入っていない」**という最も追いにくい障害になる | `22` 2.2 |
| 7 | **監査項目に `CURRENT_TIMESTAMP()` を直書きする** | ステートメントを跨ぐと値がずれ、同一実行のはずのレコードで `etl_updated_at` が食い違う | `11` 4.2 |
| 8 | **`_jst` 列に `TIMESTAMP` 型を当てる** | 型はUTC絶対時刻なのに名前はJST。JOIN・比較で9時間ずれる | `11` 4.1 |
| 9 | **取得範囲とマージウィンドウの期間がずれている** | 取得だけ広げても反映されず、ウィンドウだけ広げても取得しないので気づけない。**取込ツールが差分取得を提供していないのに、マージ側だけ差分ウィンドウを持つ**構成が典型 | `20` 2.1.1 / 3.3 / `21` 4.3 |
| 10 | **Dataformの `.sqlx` を `definitions/` 外に置く** | コンパイルされず、**テストが素通り**する。動いているように見えて何も検証されていない | `21` 5.1 |
| 11 | **`--full-refresh` の条件を `${when(incremental(), ...)}` で囲わない** | フルリフレッシュがウィンドウ内しか作り直さず、**「フルリフレッシュで復旧する」運用手順そのものが機能しない** | `21` 5.4 |
| 12 | **BRONZE原本（GCS）にライフサイクル削除を設定する** | 再構築の唯一の手段を失い、DRのRTOが無限大になる | `13` 1 / `60` 9 |

**次点（頻出）**

* `latest` タグでコンテナを本番デプロイし、障害時にどのコードが動いていたか追えない（`12` 3）
* Schedulerのリトライを有効にしたままWorkflows側でもリトライし、二重起動する（`22` 2.3）
* `SELECT *` により上流の列追加で下流のビューが壊れる（`11` 5）
* スキーマ自動検出を本番で使い、実行ごとに型が変わる（`13` 3）
* IAMをコンソールで手動付与し、Terraformの次の `apply` で消える（`30` 1）
* `MERGE` が `etl_loaded_at` まで更新し、「初回挿入日時」が毎回上書きされる（`21` 1 ルール9）
* **API有効化の伝播を待たずにapplyし、初回だけ `API has not been used in project ...` で落ちる**（`40` 9）
* **APIリストをスクリプトとTerraformで二重管理し、片方が古いまま気づかない**（`40` 9）
* **`templatefile()` で読むYAMLの「コメント」に `${...}` を書き、plan時にエラーになる**（`22` 2.2 ルール10）
* **共通サブワークフローに一律のリトライを置き、実行履歴への `INSERT` まで再実行される**（`22` 2.2 ルール2）
* **取込ツールの取込特性を確認せずに更新方式を決め、方式ごと作り直しになる**（`20` 2.1.1）

## 2. レビューチェックリスト

### 2.1 テーブル設計のPR

- [ ] データセット名が `{layer}_{option_name}` に従っている（`11` 1）
- [ ] テーブル名の接頭辞が正しい（`d_` / `f_{grain}_` / `v_` / `t_`）、単数形である
- [ ] ファクトにパーティションがある。粒度が保持年数から妥当（10年以上なら `MONTH`）
- [ ] ディメンションにクラスタがある。ファクトは基準日列が第1クラスタ列（`MONTH` の場合）
- [ ] `require_partition_filter = true` になっている
- [ ] 主キーが `{entity}_id`、コード列が `_cd`、真偽値が `is_`/`has_`
- [ ] 日時列に `_utc` / `_jst` があり、型が `TIMESTAMP` / `DATETIME` で対応している
- [ ] 数値列に単位サフィックスがある。件数列は `_count`、量は `_qty`（`11` 4.1）
- [ ] 監査項目5点（SILVER）がすべてある
- [ ] 全列に `description` がある
- [ ] 派生指標がSILVERに入っていない（`GROUP BY` / ウィンドウ関数がない）
- [ ] 本番テーブルに `deletion_protection = true`

### 2.2 取込処理のPR

- [ ] **取込ツール／コネクタの取込特性を確認し、設計書に書いた**（提供される取込方式 / 差分の指定方法 / 削除同期の可否 / GA・プレビュー・後から変更できない設定）（`20` 2.1.1）
- [ ] **更新方式が、データ量ではなくまずツールの制約から選定されている**（`20` 3.2 ステップ0）
- [ ] **全件洗替（P1）の場合、MERGE前に件数の下限チェックと急減ガードがある**（`20` 3.3）
- [ ] BRONZE監査項目を付与できない連携方式の場合、代替手段（`ops_*` の実行履歴）を設計書に明記した（`11` 4.2 ルール6）
- [ ] GCSに原本を落としてからロードしている
- [ ] 同じ `run_id` で再実行しても結果が変わらない（冪等）
- [ ] GCSパスが `13` 章の規約（`{source}/{object}/dt=.../{run_id}/...`）に従っている
- [ ] 既存オブジェクトを上書きしていない
- [ ] リトライ（429/5xx、指数バックオフ）とタイムアウトがある
- [ ] 認証情報がSecret Manager経由（環境変数直書き・コード埋め込みがない）
- [ ] スキーマを明示指定している（`autodetect` でない）
- [ ] 取得0件の扱いが設計されている
- [ ] 構造化ログに `run_id` / `record_count` が出ている
- [ ] `start_date` / `end_date` で期間指定の再実行ができる
- [ ] READMEにバックフィル手順が書かれている

### 2.3 変換（Dataform / dbt）のPR

- [ ] `SELECT *` を使っていない
- [ ] プロジェクトID・データセット名をハードコードしていない（`ref` / `${}` で解決）
- [ ] 監査項目を共通マクロで付与している（手書きしていない）
- [ ] `etl_loaded_at` が `MERGE` の更新対象から外れている（dbt: `merge_exclude_columns` / Dataform: `COALESCE` で引き継ぎ）
- [ ] `CURRENT_TIMESTAMP()` を直書きしていない
- [ ] 値の比較に `IS DISTINCT FROM` を使っている
- [ ] **範囲限定型なら、範囲条件が①ソース `WHERE` ②`ON` 句 ③`NOT MATCHED BY SOURCE` の3か所すべてに同一の式である**
- [ ] ウィンドウがソース側の処理単位の境界に丸められている
- [ ] ウィンドウ幅 ≧ ソースが遡って訂正しうる最大期間、かつ取得範囲と一致している
- [ ] 主キー一意性・NOT NULL のテスト/アサーションがある
- [ ] 全件洗替型で `NOT MATCHED BY SOURCE` を使う場合、取得失敗時に一括論理削除されない安全装置が上流にある（`20` 3.3）
- [ ] 差分ウィンドウを持つ場合、**取込側が本当にその範囲を取得している**（`20` 2.1.1）
- [ ] Dataformの場合、`.sqlx` が `definitions/` 配下にある
- [ ] Dataformの場合、ソース側条件が `${when(incremental(), ...)}` で囲まれている
- [ ] `tags` が `{layer}` と `{layer}_{option_name}` の2つ付いている

### 2.4 インフラ（Terraform）のPR

- [ ] `terraform plan` の差分がPR本文にある
- [ ] リソースのTerraform識別子が実リソース名と一致している
- [ ] 環境差分が `.tfvars` のみで表現されている（`.tf` に環境分岐がない）
- [ ] 全変数に `type` と `description` があり、列挙値に `validation` がある
- [ ] ラベル5点（`system` / `env` / `layer` / `owner` / `managed_by`）が付いている
- [ ] シークレットの「値」をtfstateに入れていない（避けられない場合はADRがある / `30` 5 ルール6）
- [ ] 機密を含む tfvars が `*.secret.tfvars` で `.gitignore` 対象になっており、`*.secret.tfvars.example` はコミットされている（`30` 6）
- [ ] APIリストを二重管理していない（1ファイルを正にし、スクリプトとTerraformの両方が読む）（`40` 9）
- [ ] API有効化をブートストラップ側で先に行い、伝播を確認してからapplyする手順になっている（`40` 9）
- [ ] IAMカスタムロールを作った場合、既定ロールで足りない理由が説明でき、命名が lowerCamelCase になっている（`12` 1）
- [ ] GCSバケットの `lifecycle_rule` が `SetStorageClass`（原本に `Delete` を書いていない）
- [ ] `uniform_bucket_level_access` / `public_access_prevention` が有効
- [ ] 基本ロール（owner/editor/viewer）を付与していない
- [ ] SAキーJSONを発行していない

### 2.5 オーケストレーションのPR

- [ ] Schedulerが起動するのはWorkflowsのみ
- [ ] `run_id` / `etl_ts` がWorkflows先頭で1回だけ採番されている
- [ ] 全ステップにタイムアウトがある
- [ ] エラーハンドラがあり、失敗通知に `run_id` とログURLが含まれる
- [ ] リトライが冪等なステップにのみ設定されている
- [ ] Schedulerのリトライが0回
- [ ] Functionsから別のFunctionsを直接呼んでいない
- [ ] 二重起動の防止がある
- [ ] Workflows YAML が構文的に妥当（式の複数行折り返し・式内の `": "` は不正YAMLになる）
- [ ] `templatefile()` で読む場合、Workflows自身の式が `$${...}` にエスケープされている（**コメント行も対象**）
- [ ] BigQueryを呼ぶステップで戻り値の `jobComplete` を検査している（`22` 2.2 ルール11）
- [ ] 共通サブワークフローに一律のリトライを置いていない（リトライは呼び出し側で指定）（`22` 2.2 ルール2）

### 2.6 リリース前（本番デプロイ）

- [ ] `stg` で同じ変更が動作確認済み
- [ ] 破壊的変更の下流影響（ビュー・BI・外部連携）を洗い出し、関係者に周知済み
- [ ] 切り戻し手順が書かれている（コードとインフラの両方）
- [ ] 監視・アラートが新しいリソースをカバーしている
- [ ] Runbookが更新されている

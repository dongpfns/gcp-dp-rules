# 60. 非機能要件

各項目は「**何を決めるか**」と「**既定値（決められない場合の初期値）**」をセットで定義する。プロジェクト開始時にこの章を埋めることを必須とする。

## 1. 可用性・信頼性

| 項目 | 決めること | 既定 |
| :--- | :--- | :--- |
| パイプライン成功率 | 月次のジョブ成功率目標 | 99.0%（月あたり失敗3回まで） |
| リトライ方針 | 自動リトライの回数・間隔 | 指数バックオフ、最大3回、初回10秒（Workflows側で制御） |
| 単一障害点 | 手動作業に依存する箇所の有無 | ゼロ（手動があるならADRに記録し、代替手順を用意） |
| リージョン障害 | 対応の要否 | 単一リージョン（`asia-northeast1`）。マルチリージョン化はコストに見合う場合のみ |

* **リトライしてはいけない処理を明示する**（冪等でない追記型ロードなど）。

## 2. 性能

| 項目 | 決めること | 既定 |
| :--- | :--- | :--- |
| バッチ完了時刻（SLO） | 「何時までに使える状態か」 | 日次: JST 08:00 まで / 時次: 起動から30分以内 |
| BIクエリ応答 | 代表クエリのレスポンス | 95パーセンタイルで10秒以内 |
| データ鮮度 | ソース発生からGOLD反映までの遅延 | 日次パイプライン: 24時間以内 |
| スキャン量 | 1クエリあたりの上限 | GOLDビュー経由の代表クエリで100GB以下 |

**性能が出ないときの標準対処（この順に試す）**

1. パーティションプルーニングが効いているか（`require_partition_filter` / クエリの `WHERE`）
2. クラスタ列が実際の絞り込み条件と合っているか
3. ビューのネストが深すぎないか（2段まで）
4. GOLDを `t_` として物理化する
5. BI Engine の予約を検討する

## 3. コスト

| 項目 | 決めること | 既定 |
| :--- | :--- | :--- |
| 料金モデル | オンデマンド / Editions（スロット予約） | オンデマンドで開始。月額が予約下限を超えたらEditionsを検討 |
| 月次予算 | プロジェクト単位の予算とアラート | 予算アラートを 50% / 80% / 100% に設定（必須） |
| クエリ上限 | ユーザー単位・プロジェクト単位のカスタムクォータ | 1ユーザー1日 1TB（本番）/ 500GB（開発） |
| ストレージ | 論理課金 / 物理課金 | 圧縮率が高いテーブルが多い場合は物理課金を試算して選ぶ |

**コスト削減の優先順位**

1. `SELECT *` の排除・列の絞り込み（最も効く）
2. パーティションプルーニング
3. GOLDの物理化（同じ集計を毎回走らせない）
4. マテリアライズドビュー
5. ストレージクラスの移行（GCS）

* **月次のコストレポート**を `INFORMATION_SCHEMA.JOBS` から作成し、`ops_audit` に蓄積して上位クエリを可視化する。

## 4. 監視・アラート

| 監視対象 | 検知方法 | 通知 |
| :--- | :--- | :--- |
| Workflows の失敗 | ログベース指標 + アラートポリシー | 即時（チャット + メール） |
| Workflows の遅延 | SLO時刻を過ぎても完了していない | 即時（チャット） |
| 取込件数の急変 | 品質チェックSQL（前回比 ±50%） | 即時（チャット） |
| データ鮮度 | `MAX(etl_updated_at)` が閾値より古い | 即時（チャット） |
| コスト急増 | 予算アラート + 日次スキャン量 | 日次サマリ |
| Functions のエラー率 | Cloud Monitoring | 5分間で5%超なら通知 |

* **アラートは「対応が必要なものだけ」鳴らす**。成功通知は日次サマリにまとめる。
* 全アラートに**対応手順（Runbook）へのリンク**を含める。手順のないアラートは作らない。

## 5. ログ

| 種別 | 保管先 | 保持 |
| :--- | :--- | :--- |
| アプリケーションログ（取込処理） | Cloud Logging（構造化JSON） | 30日 |
| ジョブ実行履歴 | BigQuery `ops_audit.job_run` | 3年 |
| データアクセス監査ログ | BigQuery `ops_audit.cloudaudit_*`（ログシンク） | 1年以上（要件次第） |
| 品質チェック結果 | BigQuery `ops_dataquality.check_result` | 1年 |

**構造化ログの必須フィールド**

```json
{"severity":"INFO","run_id":"20260904T060000Z-wf-main-daily","component":"ingest_salesforce",
 "object":"account","event":"load_completed","record_count":12345,"duration_ms":8210,
 "gcs_uri":"gs://example-prd-raw/salesforce/account/dt=2026-09-04/..."}
```

* `run_id` を全ログに含め、1回の実行を横断で追えるようにする。
* **個人情報・認証情報をログに出さない**。レスポンスボディの全文出力を禁止する。

## 6. データ品質

| 分類 | チェック内容 | 実行タイミング | 失敗時 |
| :--- | :--- | :--- | :--- |
| 完全性 | 件数0でない / 前回比の急変がない | BRONZEロード直後 | 後続を停止 |
| 一意性 | 主キーの重複がない | SILVER生成後 | 後続を停止 |
| 妥当性 | NOT NULL / コード値が定義内 / 数値が範囲内 | SILVER生成後 | 後続を停止 |
| 整合性 | ファクトのキーがディメンションに存在する | SILVER生成後 | 警告（件数が閾値超なら停止） |
| 鮮度 | ソースの最大更新日時が想定内 | BRONZEロード直後 | 後続を停止 |
| 業務ルール | 重複コマ、収支の異常値、ウィンドウ外更新の検知 | SILVER/GOLD生成後 | 警告 |

* 結果は `ops_dataquality.check_result`（`run_id` / `check_name` / `target` / `status` / `actual` / `threshold` / `checked_at_utc`）に必ず記録する。
* **「停止」と「警告」を明確に分ける**。全部停止にするとオオカミ少年になり、全部警告にすると壊れたデータが下流へ流れる。

## 7. 保守性

* 全SILVER/GOLDテーブル・列に `description` を必須とする（`11` 章 4.1）。
* 各パイプラインのREADMEに、**依存関係図・SLO・バックフィル手順・障害時の連絡先**を書く。
* Runbook（`docs/runbook/`）に、アラートごとの1次対応手順を書く。
* データオーナー（業務側の責任者）をデータセット単位で明記する。

## 8. セキュリティ・コンプライアンス

* `30` 章に準拠。加えて以下を決める。

| 項目 | 決めること | 既定 |
| :--- | :--- | :--- |
| 個人情報の扱い | どの列が個人情報か、どこでマスクするか | 取込時にハッシュ化を第一候補 |
| 監査ログ保持 | 法定・社内規程上の保持期間 | 1年（要件により延長） |
| 暗号化 | Google管理鍵 / CMEK | Google管理鍵。要件がある場合のみCMEK |
| アクセスレビュー | IAM棚卸しの頻度 | 半期に1回 |

## 9. 事業継続（DR）

| 項目 | 決めること | 既定 |
| :--- | :--- | :--- |
| RPO（目標復旧時点） | どこまでのデータ喪失を許容するか | 24時間（日次バッチの1回分） |
| RTO（目標復旧時間） | 何時間で復旧するか | 8時間 |
| 復旧手段 | 誤削除・誤更新からの戻し方 | ① BigQuery Time Travel（7日、既定）<br>② テーブルスナップショット（重要テーブルは日次、30日保持）<br>③ GCS原本からの再構築（最終手段） |

* **`13` 章の「原本を削除しない」ルールが③を成立させている**。原本を消すとRTOが無限大になる。
* Time Travel の7日を超える復旧が必要なテーブルは、テーブルスナップショットを Terraform で定義する。
* **年1回、GCS原本からの再構築を実際に試す**（手順が動くことの確認）。

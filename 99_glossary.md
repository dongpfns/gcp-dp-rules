# 99. 用語集・プレースホルダ読み替え表

## 1. プレースホルダ読み替え表

サンプルコード中の値を実プロジェクトへ適用する際の読み替え。

| プレースホルダ / サンプル値 | 意味 | 読み替え例 |
| :--- | :--- | :--- |
| `example` | システム識別子。GCPプロジェクトID・バケット名の接頭辞 | 実際のシステム名（`salesdwh`, `crmhub`） |
| `{env}` / `prd` `stg` `dev` | 環境コード | 実際の環境コード体系 |
| `{system}-{env}` / `example-prd` | GCPプロジェクトID | 実際のプロジェクトID |
| `salesforce` / `box` / `pos_api` / `file_csv` | 連携元システム（`bronze_*` の `{source_system}`） | 実際の連携元 |
| `common` / `sales` / `customer` | 業務ドメイン（`silver_*` の `{domain}`） | 実際のドメイン分割 |
| `bi_tool` / `finance` / `ml` | 利用者（`gold_*` の `{consumer}`） | 実際のBIツール名・部署名 |
| `d_account` / `Account` | ディメンションのサンプル。CRMの取引先マスタを想定 | 実際のマスタ（顧客・商品・拠点） |
| `f_30m_sales_transaction` | ファクトのサンプル。30分粒度の売上トランザクション | 実際の時系列データ |
| `shipment_volume_ton` / `return_volume_kg` / `unit_price_jpy_per_kg` | 単位サフィックスのサンプル。**同一テーブルに単位の異なる数値列（ton / kg）が混在するケースを意図的に例示**している | 実際の計測値と単位 |
| `data-platform-team` | オーナーラベルの値 | 実際のチーム名 |

## 2. 用語集

| 用語 | 意味 |
| :--- | :--- |
| **BRONZE / RAW** | 外部ソースの生データをそのまま格納する層。再構築の起点 |
| **SILVER / DWH** | 型・表記・タイムゾーンを標準化した共通利用層 |
| **GOLD / MART** | 特定の利用者向けに結合・集計を済ませた最終成果物層 |
| **ディメンション（`d_`）** | 属性・マスタデータ。時間軸を持たない |
| **ファクト（`f_`）** | 数値・時系列の実績データ。集計の主軸 |
| **粒度（grain）** | ファクトの1行が表す時間・単位（`30m` / `1d` など） |
| **ELT** | 取込後にDWH内で変換する方式（本規約の原則） |
| **ETL** | 取込前に変換する方式（本規約では例外的に許容） |
| **CDC** | Change Data Capture。差分検知。本規約では特に「物理削除の検知」を指す |
| **論理削除（`is_deleted`）** | 行を消さずフラグで削除を表現すること。物理削除はしない |
| **範囲限定型（ローリングウィンドウ）** | 直近N期間のみを毎回洗い替える差分反映方式（`20` 章 P2） |
| **冪等（idempotent）** | 同じ入力で何度実行しても結果が同じであること |
| **バックフィル** | 過去期間のデータを遡って再生成すること |
| **パーティションプルーニング** | `WHERE` 条件により読むパーティションを絞り、スキャン量を減らす仕組み |
| **Authorized View** | 基テーブルへの権限を渡さずに、ビュー経由でのみデータを公開する仕組み |
| **Time Travel** | BigQueryが過去7日間の状態を保持し、誤操作から復旧できる機能 |
| **run_id / batch_id** | 1回のパイプライン実行を識別するID。全ログ・全監査列に埋める |
| **SLO** | Service Level Objective。「日次データが08:00までに使える」等の目標 |
| **RPO / RTO** | 目標復旧時点 / 目標復旧時間（`60` 章 9節） |
| **ADR** | Architecture Decision Record。規約からの逸脱や設計判断の記録 |
| **ドリフト** | Terraformの定義と実際のリソースの乖離。手動変更で発生する |

## 3. 参考：BigQueryの主要な上限（設計に影響するもの）

| 項目 | 上限 | 影響 |
| :--- | ---: | :--- |
| 1テーブルあたりのパーティション数 | 10,000 | 日単位で約27年 |
| **単一ジョブが変更できるパーティション数** | **4,000** | **日単位で約11年。フルリフレッシュ不能になる** |
| クラスタ列の数 | 4 | 絞り込み条件の設計 |
| 1クエリの最小課金 | 10 MB | 小さいパーティションでは粒度を細かくしても安くならない |
| Time Travel | 7日（2〜7日で設定可） | これを超える復旧はスナップショット/原本から |
| テーブルあたりの列数 | 10,000 | 極端に横長なソースは列を間引く（`20` 章 ETL例外3） |
| クエリの実行時間 | 6時間 | 超える処理は分割する |
| ロードジョブのサイズ | 15 TB / job | 大量ロードは分割する |

* 上限値はGoogleにより変更されうる。設計判断の根拠にする場合は公式ドキュメントで最新値を確認すること。
